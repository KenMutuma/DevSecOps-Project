apiVersion: v1
kind: Service
metadata:
  name: netmirror-app
  labels:
    app: netmirror-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: netmirror-app
